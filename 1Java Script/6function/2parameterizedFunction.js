// you can pass value or not the code won't show any error
// if parameters are not passed means it will consider the value as undefined

function sum(name){
    console.log(`hello ${name}`);
}
sum('vijay')
sum()
