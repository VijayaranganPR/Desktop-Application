// the console.assert() method is used to test if a given condition is true or false. If the condition is true, nothing happens and the script continues to execute. 
// If the condition is false, an error message is logged to the console and the script continues to execute.


console.assert(10>20, "Failed message")
// Assertion failed: Failed message
console.assert(10<20, "won't get execute")
